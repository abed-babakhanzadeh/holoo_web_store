Start Celery:
python -m celery -A config worker -l info --pool=solo

شلیک مجدد تسک از Shell
python manage.py shell
from holoo.tasks import sync_products_from_holoo
sync_products_from_holoo.delay()
exit()

python manage.py runserver 0.0.0.0:7000

-----------------------------------------------------------------------------------------------------------------------
# ۱. کد رو بیار
git clone https://github.com/abed-babakhanzadeh/holoo_web_store.git
cd holoo_web_store
git checkout feature/arino-theme-integration-phase1

# ۲. محیط پایتون
python -m venv venv
pip install -r requirements.txt

# ۳. پیش‌نیاز سیستمی: ODBC Driver 18 for SQL Server باید نصب باشه
#    (دانلودش از سایت مایکروسافت؛ pip نصبش نمی‌کنه، pyodbc فقط ازش استفاده می‌کنه)

# ۴. یه دیتابیس MSSQL خالی روی همون سیستم بسازید، بعد config/settings.py
#    رو باز کنید و بخش DATABASES رو با آدرس/یوزر/پسورد همون SQL Server محلی خودتون هماهنگ کنید
#    (الان روی 127.0.0.1 با یوزر sa تنظیم شده - مال همین سیستم فعلیه)

# ۵. جدول‌ها رو بساز
python manage.py migrate

# ۶. کاتالوگ واقعی هلو رو بکش تو (چند دقیقه طول می‌کشه، ۵۹۷۳ کالا)
python manage.py sync_holoo_products

# ۷. (اختیاری) یوزر ادمین برای پنل جنگو
python manage.py createsuperuser